<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
	xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-2.0.xsd
        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

	<property name="add.schema" value="dbo." dbms="mssql" />
	<property name="add.schema" value="" dbms="oracle" />
	<property name="add.schema" value="" dbms="postgresql" />
	<property name="call.substr" value="substring" dbms="mssql" />
	<property name="call.substr" value="substr" dbms="oracle" />
	<property name="call.substr" value="substring" dbms="postgresql" />
	<property name="call.auditdb" value="PLFAVANSEQC.." dbms="mssql" />
	<property name="call.auditdb" value=" " dbms="oracle" />
	<property name="call.concatenate" value="+" dbms="mssql" />
	<property name="call.concatenate" value="||" dbms="oracle" />
	<property name="call.concatenate" value="||" dbms="postgresql" />
	<property name="call.collength" value="LEN" dbms="mssql" />
	<property name="call.collength" value="LENGTH" dbms="oracle" />
	<property name="call.collength" value="LENGTH" dbms="postgresql" />
	<property name="call.date" value="getdate()" dbms="mssql" />
	<property name="call.date" value="sysdate" dbms="oracle" />
	<property name="call.date" value="now()" dbms="postgresql" />

	<changeSet id="pre_run_for_pgp" author="Adarsh" runAlways="true"
		dbms="postgresql">
		<sql>
			Set search_path = plf,public,pg_catalog;
		</sql>
	</changeSet>

	<changeSet id="1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="seqauth_limit_details" />
	</changeSet>
	<changeSet id="1.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="seqauth_limit_details">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

	<changeSet id="2" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="seqauth_limits" />
	</changeSet>
	<changeSet id="2.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="seqauth_limits">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

	<changeSet id="3" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="Seqbusiness_vertical" />
	</changeSet>
	<changeSet id="3.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="Seqbusiness_vertical">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

	<changeSet id="4" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="SeqClusters" />
	</changeSet>
	<changeSet id="4.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="SeqClusters">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

	<changeSet id="5" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="seqdmsidentification" />
	</changeSet>
	<changeSet id="5.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="seqdmsidentification">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

	<changeSet id="6" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="SeqFeeWaiverDetail" />
	</changeSet>
	<changeSet id="6.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="SeqFeeWaiverDetail">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

	<changeSet id="7" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="SeqFeeWaiverHeader" />
	</changeSet>
	<changeSet id="7.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="SeqFeeWaiverHeader">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

	<changeSet id="8" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="SeqFinLiabilityReq" />
	</changeSet>
	<changeSet id="8.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="SeqFinLiabilityReq">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

	<changeSet id="9" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="SeqFinODAmzTaxDetail" />
	</changeSet>
	<changeSet id="9.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="SeqFinODAmzTaxDetail">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

	<changeSet id="10" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="SeqOrganizations" />
	</changeSet>
	<changeSet id="10.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="SeqOrganizations">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

	<changeSet id="11" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="SeqOrgIncomeExpenseHeader" />
	</changeSet>
	<changeSet id="11.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="SeqOrgIncomeExpenseHeader">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

	<changeSet id="12" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="SeqOrgIncomeExpenses" />
	</changeSet>
	<changeSet id="12.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="SeqOrgIncomeExpenses">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

	<changeSet id="13" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="SeqPostingId" />
	</changeSet>
	<changeSet id="13.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="SeqPostingId">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

	<changeSet id="14" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="SeqReportingManager" />
	</changeSet>
	<changeSet id="14.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="SeqReportingManager">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

	<changeSet id="15" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<dropSequence sequenceName="seqVASProviderAccDetail" />
	</changeSet>
	<changeSet id="15.1" author="vijaya.a" dbms="mssql">
		<comment>Converting the sequence objects into special sequence tables, since there is no support on Sequence Objects for SQL Server in our frame work</comment>
		<createTable tableName="seqVASProviderAccDetail">
			<column name="SeqNo" type="bigint" autoIncrement="true" />
			<column name="Value" type="int"></column>
		</createTable>
	</changeSet>

</databaseChangeLog>